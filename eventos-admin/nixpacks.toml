# Railway/Nixpacks: deploy do painel admin de eventos (React + Vite SPA)
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
